manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: garethbradley
      url-base: https://github.com/garethbradley
    # Additional modules containing boards/shields/custom code can be listed here as well
    # See https://docs.zephyrproject.org/3.2.0/develop/west/manifest.html#projects
  projects:
    # ZMK v0.3 + darknao's per-key RGB (rgb-layer-24.12) + LED data-line parking + layer overlay + idle dim on USB + split animation sync & effect preview.
    # Branch: garethbradley/zmk@maizeglow
    - name: zmk
      remote: garethbradley
      revision: 07151788dafcaa60ee24454556116da92fec3da2
      import: app/west.yml
  self:
    path: config
