on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # garethbradley/zmk@maizeglow (Node 24 actions, ubuntu-24.04); keep in sync with config/west.yml
    uses: garethbradley/zmk/.github/workflows/build-user-config.yml@07151788dafcaa60ee24454556116da92fec3da2
