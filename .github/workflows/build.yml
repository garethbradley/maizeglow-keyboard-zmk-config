on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # garethbradley/zmk@maizeglow (Node 24 actions, ubuntu-24.04); keep in sync with config/west.yml
    uses: garethbradley/zmk/.github/workflows/build-user-config.yml@23a44a6a93894aac91bc455899c8e6aee3fab038
